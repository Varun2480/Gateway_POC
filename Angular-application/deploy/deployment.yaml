apiVersion: apps/v1
kind: Deployment
metadata:
  name: angular-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: angular-app
  template:
    metadata:
      labels:
        app: angular-app
    spec:
      containers:
        - name: angular-container
          image: varuntej2480/frontend-svc:1.3
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: angular-config